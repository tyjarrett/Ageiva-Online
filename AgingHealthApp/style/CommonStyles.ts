import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
  safeAreaView: {
    flex: 1,
  },
  center: {
    alignItems: "center",
    justifyContent: "center",
  },
});
